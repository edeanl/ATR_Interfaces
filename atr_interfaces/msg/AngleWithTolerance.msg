# Angle between ATRs (rad)
float64 a 0

# Tolerance [0-1]
# max_angle = a * (1 + tolerance)
# min_angle = a * (1 - tolerance)
# tolerance = -1 is reserved to indicate that there is no constraint (a should be ignored)
float64 tolerance -1